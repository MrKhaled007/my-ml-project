line-length = 100

[lint]
select = ["E", "F", "I"]
ignore = [
    # Multiple statements on one line — used intentionally in the Streamlit
    # app for vertically aligned `if FLAG: missing.append(...)` blocks.
    "E701",
    # Line-too-long: keep readable long strings/dicts unwrapped.
    "E501",
]
